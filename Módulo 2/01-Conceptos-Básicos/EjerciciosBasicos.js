
/** Ejercicios Operadores Logicos
 *  a) A > 3                     b) A > C                    c) A < C
 *  d) B < C                     e) B != C                   f) A == 3
    g) A * B == 15               h) A * B == -30             i) C / B < A
    j) C / B == -10              k) C / B == -4              l) A + B + C == 5
    m) (A+B == 8) && (A-B == 2)  n) (A+B == 8) || (A-B == 6)
    o) A > 3 && B > 3 && C < 3   p) A > 3 && B >= 3 && C < -3
 */


/** Ejercicio #1 
 *  Hacer un programa que convierta los grados centígrados (30 grados) a grados Fahrenheit. 
 *  La fórmula es la siguiente: (C*1.8) +32
*/

/* Ejercicio #2
 * Pedir un número al usuario y escribirlo/imprimirlo sumándole 10.
*/

/** Ejercicio #3
 *  Realizar el mismo programa del punto(1), pero ahora se debe
 *  pedir el dato inicial (grados centígrados) al usuario (teclear el dato).
 */

/** Ejercicio #4
 *  Hacer un algoritmo que calcule el sueldo de un empleado dados como datos
 *  de entrada: el nombre, horas trabajadas y el valor de la hora.
 */

/** Ejercicio #5
 *  Crear un algoritmo que calcule el precio final de un producto, teniendo
 *  en cuenta el iva del 19%, el usuario debe ingresar el valor del producto
 *  sin iva y el algoritmo debe retornar el valor final.
 */

/** Ejercicio #6
 *  Se requiere conocer el área de un rectángulo. Realice un algoritmo para
 *  tal fin. El área del rectángulo se halla así: (base*altura)
 */

/** Ejercicio #7
 *  Se requiere obtener el área de una circunferencia. Realizar el algoritmo
 *  correspondiente. El área de la circunferencia se halla así: PI * R2 también
 *  PI * R *R RECUERDE QUE PI=3.1416 */

/** Ejercicio #8
 *  De un triángulo se tiene la longitud de la base y la longitud de la altura.
 *  Determine el valor de su área. El área del triángulo se halla así: (base*altura) /2
 */

/** Ejercicio #9
 *  Una modista, para realizar sus prendas de vestir, encarga las telas al extranjero. 
 *  Para cada pedido, tiene que proporcionar las medidas de la tela en pulgadas, 
 *  pero ella generalmente las tiene en metros. Realice un algoritmo para ayudar 
 *  a resolver el problema, determinando cuántas pulgadas debe pedir con base en los 
 *  metros que requiere. (1 pulgada = 0.0254 m).
 */

/** Ejercicio #10
 *  Un estacionamiento requiere determinar el cobro que debe aplicar a las personas
 *  que lo utilizan. Considere que el cobro es con base en las horas que lo disponen y que 
 *  las fracciones de hora se toman como completas. Realizar el algoritmo que permita 
 *  determinar el cobro.
 */

/** Ejercicio #11
 *  Un amigo suyo acaba de iniciar un negocio de venta de zapatos. Por ahora sólo vende 
 *  tres tipos de zapatos: sandalias, tenis y mocasines. Cada tipo de zapato lo adquiere a 
 *  un costo distinto y para venderlos, supone una ganancia del 55%. Cuando un cliente llega
 *  debe comprar de los tres tipos de zapatos y la cantidad que desee de cada uno de ellos 
 *  (si o si el cliente siempre compra los 3 tipos de zapatos). El cliente tiene derecho a 
 *  un 8% de descuento sobre la compra que realiza. Ayúdele a su amigo a crear un programa 
 *  que, para un cliente dado, muestre su nombre, el valor de la venta sin descuento, el descuento,
 *  valor de la venta con descuento y valor de la venta incluyendo IVA (venta neta final)
 */
